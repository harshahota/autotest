node {
  try {
    stage('Checkout') {
      sh 'echo "............................"'
      sh 'echo $BODY'
      sh 'echo $PAYLOAD'
      sh 'echo "............................"'
    }
  }
  catch (err) {
    throw err
  }
}